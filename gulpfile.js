var gulp = require('gulp');
var uncss = require('gulp-uncss');

gulp.task('default', function() {
  // place code for your default task here
  // todo: add rules for gulp-uncss https://www.npmjs.com/package/gulp-uncss
});
